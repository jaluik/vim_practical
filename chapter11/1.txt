foo = 1;
bar = 'a'
foobar = foo + bar

qa 进入宏录制模式，a 代表寄存到a的寄存器里面
A;[Esc] 行尾插入;
Ivar [Esc] 行首加入var和空格
q 退出寄存器模式


此时 输入:reg a 就可以看到刚才录制的宏

接下来可以使用 @a 来重复a寄存器中的操作，@@ 可以重复最近一次的寄存器操作

可以使用100@a 来重复执行@a宏100次

黄金法则： 录制一个宏时，要确保每条命令都可以重复执行

visualbell 可以控制关闭错误命令时的提示音
